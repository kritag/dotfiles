- sudo:
    - defaults:
        yum:
          options: "-q -y"
          stdout: False
          stderr: True
    - shell:
        - [dnf copr enable bonvaffel/cli-tools -y]
        - [scripts/addusertodockergrp.sh]
    - yum: [bat, bat-extras, btop, cargo, dnf-automatic, dnf-plugins-core, docker-compose, duf, eza, fd-find, fzf, gh, git, git-delta, less, lesspipe, make, moby-engine, neovim, nodejs-npm, oh-my-posh, python3, python3-pip, ripgrep, socat, sqlite, tealdeer, tree-sitter-cli, unzip, vim, vivid, wget, yp-tools, yq, zoxide, zsh]
- shell:
